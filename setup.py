from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Using machine learning to classify emails as spam or ham by looking at email headers.',
    author='Parthav Desai',
    license='MIT',
)
